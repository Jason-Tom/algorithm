#include<stdio.h>
#include<string.h>
int main(){
	int max[100],t=0,kind=0;
	memset(max, 0, sizeof(max));
	scanf("%d%d",&t,&kind);
	while(t--)
	{
		// printf("%d\n",t );
	}
	// for (int i = 0; i < 100; ++i)
	// {
	// 	printf("%d\n",max[i] );
	// }
	return 0;
}