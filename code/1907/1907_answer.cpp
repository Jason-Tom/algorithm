#include<stdio.h>
#include<string.h>
int main() {
	int max[100], v = 0, n_kind = 0;
	memset(max, 0, sizeof(max));
	scanf("%d%d", &v, &n_kind);
	// int weight[]
    printf("%d%d\n",v,n_kind);
	return 0;
}